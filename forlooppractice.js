var x = ["Hi", "Justin", "Hur"];
for (var i=0; i< x.length; i++) { //can use x.length instead of 3 because you do not have to keep changing the numbers after you add anorther array
window.alert(x[i]);
}
